//create a function theBeatlesPlay accepts two parameters-an array
//musicians and instruments. the body stores an empty array stored in a variable
function theBeatlesPlay(musicians, instruments) {
  var emptyArray = []
  for(musicians[0];)
    emptyArray.push( musicians ; instruments)
  }
    return \\the array of new strings
}

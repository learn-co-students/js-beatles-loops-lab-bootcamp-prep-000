function theBeatlesPlay (musicians, instruments) {
  var musicians = [];
  var musicianslength = musicians.length;
  for (i = 0; i < musicianslength; i++) {
    array.push(musicians[i] + " plays " + instruments[i]);
  }
  return array
}

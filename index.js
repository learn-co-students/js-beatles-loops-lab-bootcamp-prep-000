// add solution here
function theBeatlesPlay(musicians, instruments) {
  const music = []
    for(let i = 0; musicians.length(); i++ ){
    music.push(`${musicians} plays ${instruments}`)
}
return music
}